# Optional. If omitted, the app uses SQLite at ./tech_assess_backend.db
# For Render Postgres, use the service's internal database URL.
# DATABASE_URL=postgresql+psycopg2://user:password@host:5432/tech_assess_backend
